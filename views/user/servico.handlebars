{{#each erros_servico}}
<div class="alert alert-danger">{{texto}}</div>
{{else}}

{{/each}}


<h3>Nova simulação</h3>
<div class="card">
    <div class="card-body">
        <form action="/user/servico/novo" method="post">
            <label for="Boleto">Boleto: </label>
            <input type="number" name="boleto" id="boleto" placeholder="Valor do boleto" class="form-control">
            <label for="Bairro">Entrada: </label>
            <input type="number" name="entrada" id="entrada" placeholder="Valor da entrada" class="form-control">
            <label for="Parcela">Número de parcelas: </label>
            <input type="number" name="parcela" id="parcela" placeholder="Valor da entrada" class="form-control">

            <BUtton type="submit" id="enviar" class="btn btn-success mt-4">Criar simulação</BUtton>
        </form>
    </div>
</div>
